# Background services

Use this folder for production extensions such as order expiry, risk monitoring, and metrics publication. The runnable starter keeps matching synchronous through `SymbolOrderQueue` so that the core execution path remains easy to inspect and test.
